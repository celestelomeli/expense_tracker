# Test Environment Configuration used when running pytest tests locally

# Use localhost instead of 'db' for local testing
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Peanutbutter24!
DB_NAME=expense_tracker
